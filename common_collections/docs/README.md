# Common Collections Learning Docs

This folder documents the Rust collection examples in `src`.

The docs are organized by topic, not by raw line-by-line explanation. Each document explains:

- what the code is used for
- why that collection or pattern is chosen
- advantages
- disadvantages
- when to avoid it
- what to notice in the source code

Source files are mirrored under `docs/code/src`.
